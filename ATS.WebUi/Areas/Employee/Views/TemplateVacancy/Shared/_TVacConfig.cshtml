@using BEClient = ATS.BusinessEntity;
@model  ATS.BusinessEntity.SRPEntity.DynamicSRP<List<BEClient.TVac>>

@if (Model != null && Model.obj.Count() > 0)
{
    foreach (var _current in Model.obj)
    {
        @Html.Partial("Shared/_TVacAcc", _current)
    }
}